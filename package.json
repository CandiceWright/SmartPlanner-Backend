{
  "name": "server",
  "version": "1.0.0",
  "description": "server for planner",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "candicewright",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "body-parser": "*",
    "express": "*",
    "mysql": "*"
  }
}
